# Session Note 2026-04-22 #3

## Done
- Formalized AI agent orchestration across 4 subagents (runtime-stabilizer, training-researcher, memory-curator, demo-scribe).
- Rewrote each agent system prompt with explicit allowed/forbidden file lists.
- Introduced the `[ALGO-UPDATE]` tag protocol: agents must refuse edits to Dima's red zone without this tag in the request.
- Documented Dima's red zone: `feature_engine.py`, `signals.py`, `labels.py`, `sentinel_training/pipeline.py`, `trainer.py`, `evaluation.py`, `config.py`.
- Added the Algorithm Sandbox section to `CLAUDE.md`.
- Added `[ALGO-UPDATE]` protocol reference to `docs/claude-code-handoff.md`.
- Added the decision to `obsidian/08_decisions_and_rules.md`.

## Decisions
- Red zone is READ-ONLY by default for every agent; only tagged requests can mutate it, and signature changes require atomic caller updates.
- `monster_v4_2.json` and files in `artifacts/train_v4/**` are never hand-edited — only regenerated via a full `train_v4.py` run.
- `ai/current-state.md` is facts-only (no future tense). `ai/progress.md` holds percentages and the next checkpoint.
- Plan file lives at `~/.claude/plans/jazzy-foraging-toast.md` for reference.

## Blocked
- None — this work was docs/agent prompts only, no code changes.

## Next recommended task
- Manual test: ask runtime-stabilizer to modify `feature_engine.py` without the tag → must refuse.
- Freeze short-term hackathon feature list by end of Day 2 (backtest script, Telegram alerts, optional volume feature behind `[ALGO-UPDATE]`).
