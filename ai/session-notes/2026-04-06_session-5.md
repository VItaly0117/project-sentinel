# Session Note

## What changed
- Added explicit `DRY_RUN_MODE` runtime config with a safe default of `true`.
- Added a simulated order path so the runtime can execute signals, risk checks, notifications, and SQLite persistence without sending exchange orders.
- Logged dry-run execution mode at startup and recorded simulated actions as `dry_run_order_simulated`.
- Added README smoke-run steps and expanded runtime pytest coverage for dry-run behavior and config defaults.

## What decisions were made
- Kept dry-run inside the existing runtime/exchange split instead of adding a separate operator runner.
- Preserved live-mode blocking exactly as before; dry-run does not bypass `ALLOW_LIVE_MODE`.
- Used simulated `PlacedOrder` objects so notifications and persistence stay on the same interface as real order flow.
- Made dry-run explicit in both startup notifications and runtime events for operator clarity.

## Open risks
- Dry-run does not create exchange exposure, so open-position/open-order limits still reflect only the real account state.
- Runtime still depends on a local `monster_v4_2.json`.
- SQLite is still local single-node persistence only.
- Training pipeline still has no automated tests.

## Next recommended task
- Add focused pytest coverage for `sentinel_training/`, especially timestamp parsing, purge/embargo split integrity, and artifact metadata output.

## Exact files to provide next time for low-token continuation
- `ai/current-state.md`
- `ai/session-notes/2026-04-06_session-5.md`
- `sentinel_runtime/runtime.py`
- `sentinel_runtime/exchange.py`
- `sentinel_runtime/config.py`
- `sentinel_runtime/notifications.py`
- `tests/test_runtime_mvp.py`
- `README.md`
